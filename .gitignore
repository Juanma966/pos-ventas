# Dependencies
node_modules/

# Build output
dist/
build/

# Env files (secretos / config local) — excepto el .env base del frontend, que no contiene secretos
**/.env.local
**/.env.development.local
**/.env.test.local
**/.env.production.local
backend/.env
# .env de la raíz: variables de docker-compose.prod (secretos de producción)
/.env

# Backups de la base de datos
/backups/

# Configuración por cliente (secretos de cada stack) — Modelo A
/clientes/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / IDE
.DS_Store
.idea/
